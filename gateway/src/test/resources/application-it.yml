orchestrate:
  mapping: ../data/geo/mapping.yaml
  sources:
    bld:
      type: file
      options:
        dataPath: ../data/bld
    prc:
      type: file
      options:
        dataPath: ../data/prc

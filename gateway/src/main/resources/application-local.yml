logging:
  level:
    root: INFO
    nl.geostandaarden.imx.orchestrate: DEBUG

orchestrate:
  mapping: ../data/geo/mapping.yaml
  sources:
    bld:
      type: file
      options:
        dataPath: ../data/bld
